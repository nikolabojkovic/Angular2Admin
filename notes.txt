transition: ease-in-out 0.4s;

// build for production
npm run clean:dist && ng build --prod --build-optimizer --environment=prod

// defined in package.json file
npm run build:prod:opt
/////////// if port has been used already:
Find:

Kill:

kill -9 <PID>
kill $(lsof -t -i :YOUR_PORT_NUMBER)

///////////////

ng update --all --force

// new ng project with scss styling 
ng new My_New_Project --style=scss


// reorder table row and columnd 
https://www.npmjs.com/package/table-dragger

    // "rxjs-compat": "^6.3.2",


// dependences 
-git 
-nodejs
-payton
-bower
-gulp
-npm install - g sass@latest
-npm install node-sass@latest
-npm install -g gulp-sass
system needs to be restarted in order to apply installed software to variable path

// run app with production environment
npm run start:prod